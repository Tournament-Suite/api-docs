openapi: 3.0.3
info:
  title: Tournament Suite API
  version: 2.0.0
  description: >
    NOTE: This is a stub file. The full OpenAPI specification is generated on
    every deploy from the live NestJS service and published to
    https://api.gamertd.com/api/v1/docs/openapi.yaml
servers:
  - url: https://api.gamertd.com/api/v1
    description: Production
paths: {}
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
    apiKey:
      type: apiKey
      in: header
      name: X-API-Key
security:
  - bearerAuth: []
  - apiKey: []
